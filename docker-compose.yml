version: '3.3'
services:
  mysqldb:
    container_name: mysqldb
    image: mysql:latest
    volumes:
      - mysql_container:/var/lib/mysql:rw
    user: root
    restart: always
    ports:
      - 3308:3306
    environment:

       MYSQL_DATABASE: achat-db
       MYSQL_ALLOW_EMPTY_PASSWORD: 1


  spring_app:
    depends_on:
      - mysqldb
    image: wissemesprit/achat-docker
    ports:
      - 8089:8089
    restart: always
    container_name: wissem-achat-project

  front_app:
    depends_on:
      - wissem-achat-project
    image: wissemesprit/achat-front
    ports:
      - 4200:4200
    restart: always
    container_name: front-container
volumes:
  mysql_container: { }

